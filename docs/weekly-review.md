# 📚 Quizzes de Repaso Semanal – Roadmap TypeScript

Este documento contiene preguntas de repaso para cada semana del plan de proyectos.  
Cada quiz está diseñado para evaluar los fundamentos clave de TypeScript que se practicaron.

---

## Semana 1: 📝 Calculadora Básica
1. ¿Qué tipo de retorno debería tener una función que suma dos números?
   - A) `string`
   - B) `number`
   - C) `void`
   - D) `any`

2. ¿Qué sucede si no se tipa explícitamente el retorno de una función?
   - A) TypeScript lanza error.
   - B) TypeScript infiere el tipo automáticamente.
   - C) La función no compila.
   - D) El retorno siempre es `any`.

---

## Semana 2: 📋 Lista de Tareas
1. ¿Cuál es la mejor forma de definir la estructura de un objeto en TypeScript?
   - A) `class`
   - B) `interface`
   - C) `enum`
   - D) `tuple`

2. ¿Qué método de array devuelve un nuevo arreglo filtrado según una condición?
   - A) `map`
   - B) `filter`
   - C) `forEach`
   - D) `reduce`

---

## Semana 3: 🎲 Juego de Adivinanza
1. ¿Qué tipo usarías para representar niveles de dificultad (`"fácil" | "medio" | "difícil"`)?
   - A) `enum`
   - B) `union type`
   - C) `tuple`
   - D) `any`

2. ¿Qué tipo de retorno usarías en una función que solo muestra un mensaje en consola?
   - A) `string`
   - B) `void`
   - C) `boolean`
   - D) `unknown`

---

## Semana 4: 📚 Gestor de Contactos
1. ¿Qué modificador de acceso permite que una propiedad solo sea accesible dentro de la clase?
   - A) `public`
   - B) `private`
   - C) `protected`
   - D) `readonly`

2. ¿Qué ventaja tiene usar interfaces en lugar de objetos directos?
   - A) Permiten herencia múltiple.
   - B) Definen contratos claros para las estructuras.
   - C) Son más rápidas en ejecución.
   - D) No requieren tipado.

---

## Semana 5: 🛒 Carrito de Compras
1. ¿Qué utilidad tienen los generics en TypeScript?
   - A) Permiten tipar funciones y clases de forma flexible.
   - B) Reemplazan los `any`.
   - C) Solo sirven para arrays.
   - D) No tienen utilidad práctica.

2. ¿Qué tipo usarías para representar estados como `"empty" | "active"`?
   - A) `enum`
   - B) `union type`
   - C) `tuple`
   - D) `literal type`

---

## Semana 6: 🌐 Mini API con Node.js
1. ¿Qué tipo se usa para representar funciones asíncronas?
   - A) `Promise<T>`
   - B) `void`
   - C) `async`
   - D) `unknown`

2. ¿Qué archivo define la configuración principal de TypeScript en un proyecto?
   - A) `package.json`
   - B) `tsconfig.json`
   - C) `index.ts`
   - D) `config.ts`

---

## Semana 7: 🎨 App con React + TypeScript
1. ¿Cómo se tipan las props en un componente funcional?
   - A) Con `interface` o `type`.
   - B) Con `enum`.
   - C) Con `class`.
   - D) No se tipan.

2. ¿Qué tipo se usa para eventos de input en React?
   - A) `Event`
   - B) `React.ChangeEvent<HTMLInputElement>`
   - C) `MouseEvent`
   - D) `KeyboardEvent`

---

# 📍 Recomendaciones
- Responde los quizzes al final de cada semana.
- Revisa los errores y vuelve al proyecto para reforzar conceptos.
- Usa estos quizzes como base para crear tus propias preguntas.
