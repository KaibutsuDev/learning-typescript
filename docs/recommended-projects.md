# 🚀 Proyectos Recomendados para Aprender TypeScript

Este listado propone proyectos progresivos para practicar fundamentos de TypeScript, desde lo más básico hasta un nivel intermedio.

---

## 1. 📝 Calculadora Básica
**Objetivo:** Practicar tipos primitivos y funciones.  
**Fundamentos a incluir:**
- Tipado de parámetros y retorno en funciones.
- Uso de `number`, `string`, `boolean`.
- Control de flujo (`if`, `switch`).
- Manejo de errores simples (ej. división por cero).

---

## 2. 📋 Lista de Tareas (To-Do App)
**Objetivo:** Aprender a trabajar con arrays y objetos.  
**Fundamentos a incluir:**
- Tipado de arrays y objetos.
- Interfaces para definir la estructura de una tarea.
- Métodos de arrays (`map`, `filter`, `forEach`).
- Uso de `readonly` y propiedades opcionales.

---

## 3. 🎲 Juego de Adivinanza de Números
**Objetivo:** Practicar funciones y tipos especiales.  
**Fundamentos a incluir:**
- Tipado de funciones con retorno `void` y `number`.
- Uso de `Math.random()`.
- Tipos literales para definir niveles de dificultad.
- Manejo de bucles y condiciones.

---

## 4. 📚 Gestor de Contactos
**Objetivo:** Trabajar con interfaces y clases.  
**Fundamentos a incluir:**
- Definición de interfaces para contactos.
- Clases con constructores y métodos.
- Modificadores de acceso (`public`, `private`).
- Arrays de objetos tipados.

---

## 5. 🛒 Carrito de Compras
**Objetivo:** Introducir generics y tipos compuestos.  
**Fundamentos a incluir:**
- Interfaces para productos y carrito.
- Funciones genéricas para añadir/eliminar elementos.
- Uso de `union types` (ej. estado del carrito: `"empty" | "active"`).
- Tipos `readonly` para evitar mutaciones accidentales.

---

## 6. 🌐 Mini API con Node.js + TypeScript
**Objetivo:** Integrar TypeScript con un entorno real.  
**Fundamentos a incluir:**
- Configuración de `tsconfig.json`.
- Tipado de parámetros en funciones asíncronas (`Promise`).
- Uso de `async/await`.
- Tipado de respuestas y errores.
- Organización en módulos (`import/export`).

---

## 7. 🎨 Pequeña App con React + TypeScript
**Objetivo:** Aplicar TypeScript en un framework frontend.  
**Fundamentos a incluir:**
- Tipado de props y estado.
- Uso de `useState` y `useEffect` con tipos.
- Componentes funcionales con interfaces.
- Tipado de eventos (ej. `React.ChangeEvent<HTMLInputElement>`).

---

# 📍 Recomendaciones
- Empieza con proyectos **muy pequeños** (calculadora, juego de números).
- Avanza hacia proyectos con **estructuras más complejas** (carrito, gestor de contactos).
- Finalmente, integra TypeScript en **frameworks y APIs** para ver cómo se aplica en proyectos reales.
