# ✅ Respuestas y Explicaciones – Quizzes de Repaso TypeScript

Este documento contiene las respuestas correctas y una breve explicación para cada pregunta de los quizzes semanales.

---

## Semana 1: 📝 Calculadora Básica
1. **Respuesta:** B) `number`  
   ✔ Una función que suma dos números debe devolver un `number`.  
2. **Respuesta:** B) TypeScript infiere el tipo automáticamente.  
   ✔ Si no se especifica, TS deduce el tipo de retorno según el valor devuelto.

---

## Semana 2: 📋 Lista de Tareas
1. **Respuesta:** B) `interface`  
   ✔ Las interfaces definen contratos claros para la estructura de objetos.  
2. **Respuesta:** B) `filter`  
   ✔ `filter` devuelve un nuevo array con los elementos que cumplen la condición.

---

## Semana 3: 🎲 Juego de Adivinanza
1. **Respuesta:** B) `union type`  
   ✔ `"fácil" | "medio" | "difícil"` es un ejemplo de unión de literales.  
2. **Respuesta:** B) `void`  
   ✔ Una función que solo imprime en consola no devuelve nada, por lo tanto `void`.

---

## Semana 4: 📚 Gestor de Contactos
1. **Respuesta:** B) `private`  
   ✔ `private` restringe el acceso a la propiedad solo dentro de la clase.  
2. **Respuesta:** B) Definen contratos claros para las estructuras.  
   ✔ Las interfaces permiten tipar objetos y asegurar consistencia en su uso.

---

## Semana 5: 🛒 Carrito de Compras
1. **Respuesta:** A) Permiten tipar funciones y clases de forma flexible.  
   ✔ Los generics permiten reutilizar código con diferentes tipos sin perder seguridad.  
2. **Respuesta:** B) `union type`  
   ✔ `"empty" | "active"` es un ejemplo de unión de tipos literales.

---

## Semana 6: 🌐 Mini API con Node.js
1. **Respuesta:** A) `Promise<T>`  
   ✔ Las funciones asíncronas retornan promesas tipadas.  
2. **Respuesta:** B) `tsconfig.json`  
   ✔ Este archivo define la configuración principal de TypeScript en un proyecto.

---

## Semana 7: 🎨 App con React + TypeScript
1. **Respuesta:** A) Con `interface` o `type`.  
   ✔ Props se tipan con interfaces o type aliases para mayor claridad.  
2. **Respuesta:** B) `React.ChangeEvent<HTMLInputElement>`  
   ✔ Este es el tipo correcto para eventos de cambio en inputs.

---

# 📍 Recomendaciones Finales
- Usa este documento para **comparar tus respuestas** después de cada quiz.  
- Si fallas una pregunta, vuelve al proyecto correspondiente y refuerza el concepto.  
- Al terminar el roadmap, intenta crear tus propios quizzes para consolidar el aprendizaje.
